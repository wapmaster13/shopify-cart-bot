{% schema %}
{
  "name": "Cart Bot Monitor",
  "target": "body",
  "settings": []
}
{% endschema %}

<!-- CartBot Loader -->
<script>
    // We use the App Proxy path configured in shopify.app.toml
    var proxyBase = '/apps/advanced-cart-bot';
    
    // Load Rules (Proxy maps to /public/rules/...)
    loadScript(proxyBase + '/rules/' + shop + '?v=' + v);
    
    // Load Brain (Proxy maps to /public/cart-bot.js)
    loadScript(proxyBase + '/cart-bot.js?v=' + v);
    
  })();
</script>